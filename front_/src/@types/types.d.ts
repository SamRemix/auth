export type AlbumProps = {
  id: string
  title: string
  release: string
  cover: string
}

export type UserProps = {
  id: string
  name: string
  email: string
  role: string
  createdAt: string
}