import transition from '../../transition/transition'

import Container from '../../components/Container'

const Home = () => <Container title="Home" justifyContent={true} />

export default transition(Home)