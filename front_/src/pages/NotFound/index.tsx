import transition from '../../transition/transition'

import Container from '../../components/Container'

const NotFound = () => <Container title="Page not found" justifyContent={true} />

export default transition(NotFound)